# REMNANT_VAEL 프로젝트 분석 보고서 (최종)

이 보고서는 이전의 시스템 분석을 포함하여, 게임의 모든 핵심 매커니즘을 종합적으로 정리한 최종 보고서입니다.

## 1. 2.5D 및 렌더링 시스템
- **Y-Sorting 기반 깊이 제어**: `LayerOrderManager`와 `DepthController`를 통해 2D 환경에서 실시간으로 오브젝트의 정렬 순서를 변경하여 2.5D의 공간감을 구현합니다.
- **실시간 그림자**: `TrueShadow` 라이브러리를 활용하여 캐릭터와 오브젝트에 동적인 그림자를 부여, 입체감을 극대화합니다.

## 2. 스테이지 및 맵 시스템 (Stage & Map)
- **절차적/데이터 기반 생성**: `StageManager`가 `NavMeshPlus`를 활용하여 스테이지를 생성하며, `RoomController`와 `GateController`를 통해 방 단위의 이동과 규칙을 관리합니다.
- **방 규칙 (Room Rules)**: 각 방은 적 처치, 특정 오브젝트 보호 등 고유한 규칙을 가질 수 있습니다.

## 3. 전투 및 성장 시스템 (Combat & Growth)
- **무기 시스템 (`PlayerWeaponController`)**: 연사 속도(ROF), 치명타 확률(CC), 치명타 데미지(CD) 등 상세 스탯(`BUState`)을 기반으로 전투가 진행됩니다.
- **모듈 및 싱크로니 (`ModuleItemManager`)**: 
  - 장착한 모듈의 조합에 따라 **싱크로니(Synchrony)** 시너지가 활성화됩니다.
  - 전투 중 특정 타이밍(발사 시, 타격 시 등)에 인터페이스(`IWhenSync_Fire` 등)를 통해 추가 효과가 발동됩니다.
- **버프 시스템 (`BuffManager`)**: 속성 상태 이상(전기 등)과 플레이어 강화를 위한 버프/디버프를 관리합니다.

## 4. 적 AI 시스템 (Enemy AI)
- **전략 패턴 기반 AI**: `EnemyPattern` 추상 클래스를 상속받는 다양한 패턴(Follow, Melee, Range, Thrust 등)을 적들이 동적으로 실행합니다.
- **패턴 제어**: 코루틴을 사용하여 복잡한 행동 패턴을 구현하며, 특정 상황(충전 중 등)의 시각적 알림을 포함합니다.

## 5. 리소스 및 최적화 시스템
- **객체 풀링 (`PoolingManager`)**: 탄환, 타격 이펙트, 적, 아이템, UI 텍스트 등 빈번하게 생성/파괴되는 모든 요소를 `TTypePooling<T>`으로 관리하여 런타임 성능을 최적화합니다.
- **중앙 리소스 관리 (`ResourceManager`)**: CSV/JSON 데이터와 프리팹, 사운드 리소스를 통합 관리하여 데이터 중심 설계를 지원합니다.

## 6. UI 및 연출 시스템
- **중앙 집중식 UI (`MainGameUIManager`)**: HUD, 인벤토리, 상점, 퍼즐 등 다양한 UI 패널을 통합 관리하며 DOTween을 사용한 연출을 제공합니다.
- **내러티브 연출 (`EventManager`)**: 대화, 컷신, 카메라 연출 등을 데이터에 따라 순차적으로 실행하여 게임의 몰입도를 높입니다.

## 7. 시스템 최적화 과제 (Optimization Challenges)

게임의 퍼포먼스를 극대화하고 클라이언트 프로그래머로서의 기술적 역량을 증명하기 위해 다음과 같은 최적화 과제를 식별하고 해결 방향을 수립했습니다.

### [과제 1] 2.5D 뎁스 정렬 시스템의 런타임 오버헤드 해결
- **문제점 (Current Bottlenecks):**
    - **GC 할당 (GC Alloc):** 매 프레임 LINQ(`OrderBy`, `ThenBy`) 및 `.ToList()` 호출로 인해 대량의 가비지 메모리가 발생하여 정기적인 GC Spike(스터터링) 유발.
    - **비효율적 검사 로직:** 정렬 순서 변경 여부를 확인하기 위해 매 프레임 "전체 정렬($O(N \log N)$) + 전수 조사($O(N)$)"를 수행하는 구조적 모순 발생.
    - **CPU 점유:** 모든 연산이 메인 스레드에서 싱글 스레드로 동작하여 오브젝트 증가 시 프레임 드랍의 직접적인 원인이 됨.

- **해결 방안 (Proposed Solutions):**
    - **Dirty Flag 패턴 도입:** 객체의 이동이 발생했을 때만 정렬을 요청하는 Push 모델로 전환하여 불필요한 연산 횟수를 최소화.
    - **Zero-GC 정렬:** LINQ를 제거하고 `List.Sort()` 또는 `NativeArray` 기반의 In-place 정렬을 사용하여 메모리 할당을 0으로 억제.
    - **Job System & Burst Compiler:** 정렬 연산을 Worker Thread로 분산하고 Burst Compiler를 통해 기계어 수준의 최적화를 달성하여 메인 스레드 부하 분산.
    - **실행 타이밍 최적화:** 모든 이동 연산이 완료된 후인 `LateUpdate` 시점으로 실행 시기를 고정하여 렌더링 지터링 현상 방지.

## 8. 결론
**REMNANT_VAEL**은 탄탄한 최적화 구조(`PoolingManager`) 위에 깊이 있는 성장 매커니즘(`Module & Synchrony`)과 정교한 연출 시스템(`EventManager`, `TrueShadow`)이 결합된 완성도 높은 로그라이트 액션 게임입니다. 식별된 최적화 과제들을 해결함으로써 하이엔드 액션 게임에 걸맞은 퍼포먼스와 안정성을 확보할 수 있는 잠재력을 가지고 있습니다.
