
/// <summary>Build Bake에서 사용하는 전용 유틸리티 </summary>

/*
Build Bake 전용 유틸리티 클래스
현재는 별도의 계산 기능이 없어 비어 있는 상태
향후 Build Bake에 필요한 계산이나 공통 기능(예: 이름 규칙 파싱, 데이터 변환 등)을 추가하는 확장 지점
BakeUtility에는 공용 기능만 두고, Build 전용 기능은 이 클래스에 분리하여 관리
*/

public static class RuntimeBuildUtility
{
    // 현재 Build Bake는 별도의 계산이나 규칙이 없으므로
    // 추후 확장을 위해 빈 유틸리티로 유지한다.
}